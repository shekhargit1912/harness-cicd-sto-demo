apiVersion: v1
kind: Service
metadata:
  name: harness-demo-app
  namespace: harness-demo
  labels:
    app: harness-demo-app
spec:
  type: NodePort
  selector:
    app: harness-demo-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
